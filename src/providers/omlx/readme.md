# OMLX Provider

Connect to local Qwen models via the OMLX endpoint.

## Prerequisites

- A running OMLX server (local or remote)
- API endpoint accessible at `http://localhost:8000/v1`
- Valid API key (configured in your environment with .env file): OMLX_API_KEY=<yourAPIkey>
- Install dotenv package using: npm install dotenv

